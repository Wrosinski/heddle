# Plan: blocked-pending-decision

Synthetic edge-case workspace (M2 kernel read-model scaffolding) — the
coverage map row in tests/fixtures/workspaces/README.md names the state
shape this workspace exercises. state.yaml is the fact source.

### Milestone m1: Parser core

Prose stub — facts live in state.yaml.

### Milestone m2: Validator wiring

Prose stub — facts live in state.yaml.
