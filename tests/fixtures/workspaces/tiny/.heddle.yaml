# Minimal host contract — the layout.* form is canonical (D2).
layout:
  specs: docs/features
  plans: plans
  prompts: .heddle/prompts
  schemas: .heddle/schemas
  tests: tests
  source: src

commands:
  test: pytest -q
